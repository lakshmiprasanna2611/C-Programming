#include<stdio.h>
int main()
{
	int h;
	printf("enter the height:");
	scanf("%d",&h);
	if(h<150)
	printf("dwarf");
	else if((h>150) && (h<=165))
	printf("normal height");
	else if((h>165) && (h<=195))
	printf("taller");
	else
	printf("abnormal");
	
}
