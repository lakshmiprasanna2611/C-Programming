#include<stdio.h>
int main()
{
	int n1,n2,gcd,lcm,a,b;
	printf("Enter two numbers:\n");
	scanf("%d%d",&n1,&n2);
	a=n1;
	b=n2;
	while(n1!=n2){
		if(n1>n2)
		{
			n1=n1-n2;
		}
		else{
			n2=n2-n1;
}
	}
	gcd=n1;
	printf("gcd is %d\n",gcd);
	lcm=(a*b)/gcd;
	printf("lcm=%d\n",lcm);
	return 0;
}
